<%= form_for(lui) do |f| %>
  <% if lui.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lui.errors.count, "error") %> prohibited this lui from being saved:</h2>

      <ul>
      <% lui.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
